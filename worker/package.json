{
  "name": "ecosystemsca-facebook-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env devmt",
    "deploy": "wrangler deploy --env prd",
    "deploy:prd": "wrangler deploy --env prd",
    "deploy:devmt": "wrangler deploy --env devmt"
  },
  "devDependencies": {
    "wrangler": "^4.8.0"
  }
}
